# fonts/

Font assets used by the LVGL layouts.

The current firmware references the bundled Rajdhani TTF files and
`bootstrap-icons.woff` from this directory. Add new font files here and
reference them from the `font:` sections in `thermostat_480.yaml` or
`thermostat_240.yaml`.
